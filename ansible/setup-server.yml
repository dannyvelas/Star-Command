---
- name: Prepare server by hardening SSH
  hosts: vpn
  become: true
  vars:
    ssh_public_key: "{{ lookup('file', '~/.ssh/id_ed25519.pub') }}"
  vars_files:
    - secrets.yml

  tasks:
    ### Harden SSH
    - name: Harden SSH
      include_tasks: tasks/harden-ssh.yml
